[package]
name = "claim-model"
version = "2.0.3"
edition = "2021"

[lib]
name = "claim_model"

[dependencies]
near-sdk = { workspace = true }

[dev-dependencies]
# Provides the mocked host environment so the crate's unit tests can link.
near-sdk = { workspace = true, features = ["unit-testing"] }
